#!/bin/bash

echo "🚀 دليل نشر مشروع مُصَحِّح على Render.com"
echo "=========================================="
echo ""

cat << 'EOF'

📋 الخطوة 1: تحضير المشروع
══════════════════════════════

1. إنشاء حساب GitHub (إذا لم يكن لديك):
   https://github.com/signup

2. رفع المشروع على GitHub:
   
   # في مجلد المشروع
   git init
   git add .
   git commit -m "Initial commit: Arabic Corrector Platform"
   
   # إنشاء repository جديد على GitHub
   # ثم ربطه
   git remote add origin https://github.com/YOUR_USERNAME/arabic-corrector.git
   git branch -M main
   git push -u origin main


📋 الخطوة 2: إنشاء حساب Render
══════════════════════════════

1. اذهب إلى: https://render.com
2. اضغط "Get Started for Free"
3. سجل الدخول باستخدام GitHub


📋 الخطوة 3: نشر التطبيق
══════════════════════════════

1. من لوحة التحكم، اضغط "New +" ثم "Web Service"

2. اختر repository المشروع (arabic-corrector)

3. املأ التفاصيل:
   ┌────────────────────────────────────┐
   │ Name: arabic-corrector             │
   │ Region: Oregon (US West)           │
   │ Branch: main                       │
   │ Runtime: Python 3                  │
   │ Build Command:                     │
   │   pip install -r requirements.txt  │
   │ Start Command:                     │
   │   cd backend && uvicorn app:app    │
   │   --host 0.0.0.0 --port $PORT      │
   │ Instance Type: Free                │
   └────────────────────────────────────┘

4. اضغط "Create Web Service"

5. انتظر 5-10 دقائق حتى يكتمل النشر


📋 الخطوة 4: اختبار التطبيق
══════════════════════════════

سيكون التطبيق متاحاً على:
https://arabic-corrector.onrender.com

اختبار API:
curl https://arabic-corrector.onrender.com/api/health


⚠️  ملاحظات مهمة:
══════════════════════════════

1. الخطة المجانية:
   - النشر يستغرق 5-10 دقائق
   - التطبيق ينام بعد 15 دقيقة من عدم الاستخدام
   - أول طلب بعد النوم يستغرق 30 ثانية

2. لتجنب النوم (اختياري):
   - استخدم UptimeRobot.com لإرسال ping كل 5 دقائق
   - أو ترقية للخطة المدفوعة ($7/شهر)


✅ النشر اكتمل!
══════════════════════════════

التطبيق الآن متاح للعالم! 🎉

EOF
